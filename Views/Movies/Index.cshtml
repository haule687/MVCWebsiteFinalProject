@*Developed by Hau Le*@
@model FinalProject.Models.MovieGenreViewModel

@{
    ViewData["Title"] = "Movies";
}

<h1>Movies</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<form asp-controller="Movies" asp-action="Index" method="get">
    <p>
        <select asp-for="MovieGenre" asp-items="Model.Genres">
            <option value="">All</option>
        </select>
        Title: <input type="text" asp-for="SearchString" />
        <input type="submit" value="Filter" />
    </p>
</form>
<div class="container">
    @*@foreach (var item in Model)
    {
        if (att == "")
        {
            att = item.Attribute;
        }
        else if (att != item.Attribute)
        {
            check = true;
        }
    }*@
    @*<div class="row">
        @if (check == true)
        {
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                <h3 class="text-center">All</h3>
            </div>
        }
        else
        {
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12"><h3 class="text-center">@att</h3></div>
        }
    </div>*@
    <div class="row">
        @foreach (var item in Model.Movies!)
        {
            <div class="col-lg-3">
                @*Use Url.Action to get the item variable*@
                <a href="@Url.Action("Details", new { id = item.Id })">

                    <img src="@item.ImageAddress" alt="Trulli" style="width:256px;height:144px;" class="img-thumbnail" />

                </a>

                <p class="text-center">
                    @*<a href="/Edit/id">Edit</a>*@
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @*<a href="/Delete/id">Edit</a>*@
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </p>
            </div>
        }
    </div>
</div>
@*<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Movies![0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movies![0].ReleaseDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movies![0].Genre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movies![0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movies![0].Rating)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movies![0].ImageAddress)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Movies!)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReleaseDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rating)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImageAddress)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>*@
